var newEvent = "<%= j render 'events/show', postable: @event, response: @response%>";


var newPostWindow = document.querySelector('.new-post-window');

newPostWindow.insertAdjacentHTML('afterend', newEvent);

var eventForm = document.getElementById('new_event');

eventForm.reset();
