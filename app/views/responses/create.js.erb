function refreshForm(innerHTML) {
  const newResponseForm = document.getElementById('new-response');
  newResponseForm.innerHTML = "";
  newResponseForm.innerHTML = innerHTML;
  console.log(newResponseForm)
}

function addResponse(responseHTML) {
  const responses = document.getElementById('response-display');
  responses.insertAdjacentHTML('afterend', responseHTML);
  console.log(responses)
}

<% if @response.errors.any? %>
  refreshForm('<%= j render "favours/show", postable: postable, response: @response %>');
<% else %>
  addResponse('<%= j render "responses/show", response: @response %>');
  refreshForm('<%= render("responses/form", { postable: @favour, response: @response }) %>');
<% end %>
